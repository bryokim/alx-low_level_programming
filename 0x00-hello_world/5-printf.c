#include <stdio.h>

/**
 * main - prints a string to stdout.
 *
 * Description: The string "with proper grammar,
 *  but the outcome is a piece of art," is
 * printed.
 *
 * Return: The returned value is 0.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
